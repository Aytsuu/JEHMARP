-- Auth users are no longer seeded automatically.
-- Create local accounts in Studio: Authentication > Users > Add user.
-- Map app roles in public.admin_role and public.agent_profile after sign-up.

do $$ begin null; end $$;
