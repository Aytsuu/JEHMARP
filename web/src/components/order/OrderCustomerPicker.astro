---
import Picker from "@/components/ui/Picker.astro";

type Props = {
  disabled?: boolean;
  showPaymentNotice?: boolean;
  placeholder?: string;
  searchLabel?: string;
};

const props = Astro.props;
---

<Picker {...props} />
