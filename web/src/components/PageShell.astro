---
type Props = {
  eyebrow: string;
  title: string;
  description: string;
};

const { eyebrow, title, description } = Astro.props;
---

<section>
  <header>
    <p>{eyebrow}</p>
    <h1>{title}</h1>
    <p>{description}</p>
  </header>
  <div>
    <slot />
  </div>
</section>
