---
import PublicPageSections from "@/components/PublicPageSections.astro";
import PageShell from "@/components/PageShell.astro";
import Layout from "@/layouts/Layout.astro";
import { getPublicPageContent } from "@/lib/public-website/content";

const content = await getPublicPageContent(Astro, "our-story");
const title = content.page?.title ?? "Our Story";
---

<Layout title={`NMC | ${title}`} description="NMC public story page.">
  <PageShell
    eyebrow="Public website"
    title={title}
    description="Published story content is loaded from the page and page section records."
  >
    <PublicPageSections sections={content.sections} />
  </PageShell>
</Layout>
