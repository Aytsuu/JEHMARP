---
export const prerender = false;

const { orderId } = Astro.params;

if (!orderId) {
  return Astro.redirect("/admin/orders", 302);
}

const search = Astro.url.search;

return Astro.redirect(`/admin/orders/customer/${orderId}${search}`, 301);
---
