---
import AgentPageFrame from "@/components/agent/AgentPageFrame.astro";
import { formatAgentActionFeedback } from "@/lib/agent-dashboard/actions";
import { loadAgentDashboardData } from "@/lib/agent-dashboard/data";
import { requireAgentRoute } from "@/lib/agent-dashboard/page";

export const prerender = false;

const agentRoute = await requireAgentRoute(Astro);

if (!agentRoute.ready) {
  return agentRoute.response;
}

const feedback = formatAgentActionFeedback(Astro.url);
const data = await loadAgentDashboardData(Astro, agentRoute.userId);
---

<AgentPageFrame
  title="Agent Settings - JEHMARP"
  description="Manage your reseller profile and preferences."
  heading="Settings"
  intro="Update your reseller contact info, payout options, and alert thresholds."
  feedback={feedback}
  dashboardData={data}
>
  <div class="admin-panel">
    <div class="admin-card">
      <h3 class="m-0 text-base font-bold text-gray-900 mb-2">Reseller Profile Settings</h3>
      <p class="m-0 text-sm text-gray-500 mb-4">Configurations and preferences are managed here.</p>
      <div style="padding: 3rem 2rem; border: 2px dashed #e5e7eb; border-radius: 0.5rem; text-align: center; color: #9ca3af; font-size: 0.875rem;">
        Settings panel is under construction.
      </div>
    </div>
  </div>
</AgentPageFrame>
